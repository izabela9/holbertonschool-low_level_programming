#include "main.h"
/**
 * add - function that adds two integers and returns a value
 * @a : integer input
 * @b : integer input
 * Return: the sum of two integers
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
