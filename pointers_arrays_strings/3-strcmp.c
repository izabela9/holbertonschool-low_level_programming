#include <stdio.h>
#include "main.h"



int _strcmp(char *s1, char *s2)
{
    int i;
    int len = strlen(char *s1);
    
    for (i = 0; i < len; i++)
    {
        if (s1[i] - s2[i] != 0)
        return (s1[] - s2[])
    
    }
    return (0);
}
